# 도형 표시 기능 운영 반영

2026-09-22 사용자 요청으로 미리보기 기능 전체의 운영 반영을 진행한다.

## 기준과 보존 범위

- 기존 운영: dpl_GqnRQhhwvW5CsRqidLn6pUg49hsc, 소스 383462a, 운영 주소 https://mathgraph-five.vercel.app.
- 원격 main: f87468d. 기존 운영과 실제 파일 59개가 다르므로 한글 연결·사진 처리·수식/라벨 개선을 보존해 먼저 병합한다.
- 새 표시 기능: codex/geometry-annotations-preview-20260922의 638e568. 시험지 산출물 이력이 없는 공개용 브랜치만 사용한다.
- 기존 로컬 main과 원본 폴더의 미커밋 시험지는 보존한다. 로컬 main은 별도 보존 브랜치를 만든 뒤 검증된 통합본으로 맞춘다. 기존 커밋이나 원격 이력은 재작성하지 않는다.

## 순서와 완료 기준

1. 원격 main 기반 별도 작업 공간에 기존 운영을 병합한다. 소속 검산·기하 관계·한글 패키지를 모두 유지한다.
2. 표시 기능을 병합하고 전체 검사/빌드, 데스크톱·모바일 및 한글 연결 다운로드 확인을 실행한다.
3. 통합본을 main에 반영해 원격으로 푸시한다. Vercel의 Git 연결과 Production Branch를 main으로 맞춘다.
4. main의 대상 커밋을 Production에 배포한다. READY·운영 별칭·소스 커밋 일치와 일반 브라우저의 실제 조작을 확인한다.
5. 실패 시 새 운영 반영을 중단하고 직전 운영의 main 이력과 검증 결과를 사용해 복구한다. Preview 보호를 해제하거나 기능 브랜치를 Production 별칭에 연결하지 않는다.

## 지침과 검증

기존 routing-personal-playbooks(조회 unavailable), pbj-ui-copy, frontend-testing-debugging, vercel-cli 및 deployments-cicd 지침을 따른다. 환경 변수는 이름/존재 여부만 확인하며 값을 기록하지 않는다. Git/배포 상태는 실행 결과로만 완료 처리한다.

## 통합 검증 결과

- 기존 운영과 원격 main을 681bc94로 병합했다. 사진 기하 관계/참조 검증과 main의 소속 검산을 모두 유지했다.
- 표시 기능을 병합하면서 운영의 길이 호 높이·한글 수식 간격·라벨 앵커를 유지했다. 클릭 판정은 실제 렌더링 곡선을 따르고, 옮긴 라벨의 사각형에 겹치는 곡선 부분만 제거한다.
- 전체 455건과 정적 빌드가 통과했다. 실제 1440×900·1280×720·390×844에서 새 도구·속성·점 이동·실행 취소/다시 실행·프로젝트 파일 왕복·PNG/SVG 출력·입력 상한 거부·무강조색 출력을 확인했다. 브라우저 오류 0건이다.
- 기존 운영 대비 AIService, ProblemComposer, HwpDiagram, runtime, 한글 패키지 빌드와 workspace.css를 그대로 유지했다. 한글 연결 ZIP 다운로드의 정상 응답도 별도로 확인한다.

## main과 배포 연결

- 통합 커밋 9641634를 로컬·원격 main에 반영했다. 기존 운영 383462a와 이전 원격 main f87468d가 모두 조상으로 유지된다.
- 기존 로컬 main 275d9ec은 `codex/local-main-preserved-20260922`에 보존했다. 원본 작업 폴더와 시험지 미커밋 변경은 건드리지 않았다.
- Vercel의 Git 연결을 BeomjinSouth/mathgraph로 복구했고 실제 `link.productionBranch` 값이 `main`임을 확인했다. 운영용 환경 변수 세 개는 암호화 상태로 존재한다.
- 한글 연결 패키지의 HTTP 200·50,205바이트·ZIP 서명을 확인했다. 기존 앱 기능 파일 삭제는 없다.
- 이 문서 커밋의 main 푸시로 Production 자동 배포를 요청한다. READY·운영 별칭·소스 커밋·실제 브라우저 확인 결과는 배포 검증 기록에 남긴다. 검증 전에는 운영 완료로 간주하지 않는다.
